class OCRService:

    @staticmethod
    async def extract_text(file_path: str):

        print(f"OCR STARTED: {file_path}")

        # REAL OCR LATER

        extracted_text = (
            "This is extracted OCR text"
        )

        return extracted_text